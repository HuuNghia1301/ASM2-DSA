class Node {
    int data; // Dữ liệu của phần tử
    Node next; // Con trỏ tới phần tử tiếp theo

    // Constructor khởi tạo Node
    public Node(int data) {
        this.data = data;
        this.next = null; // Mặc định con trỏ next là null
    }
}
